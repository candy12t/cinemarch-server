package handler

import "testing"

func TestCinemaHandler_Show(t *testing.T) {
	t.Skip()
	tests := []struct {
		name string
	}{
		{
			name: "",
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
		})
	}
}
